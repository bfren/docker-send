version: "3.8"

services:
  send:
    image: bfren/send
    container_name: send-server
    restart: unless-stopped
    environment:
      - SEND_BASE_URL=${BASE_URL}
    volumes:
      - ./v/uploads:/uploads
    networks:
      - proxy
      - send
  redis:
    image: bfren/redis:redis7
    container_name: send-redis
    restart: unless-stopped
    networks:
      - send

networks:
  send:
    driver: bridge
    name: send
  proxy:
    external: true
